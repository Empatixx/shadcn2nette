{* Generated from shadcn/ui ChartLegendContent by shadcn2nette. Do not edit by hand. *}
{default $hideIcon = 'false'}
{default $itemConfig = null}
{default $verticalAlign = 'bottom'}
{default $class = ''}
<div class="flex items-center justify-center gap-4 {$verticalAlign === 'top' ? 'pb-3' : 'pt-3'} {$class}">
	{foreach payload
          .filter((item) => item.type !== "none") as $item}
		<div class="flex items-center gap-1.5 [&>svg]:h-3 [&>svg]:w-3 [&>svg]:text-muted-foreground">
			{if $itemConfig->icon && !$hideIcon}
				<div />
			{else}
				<div class="h-2 w-2 shrink-0 rounded-[2px]" style="{{
                      backgroundColor: item.color,
                    }}" />
			{/if}
			{$itemConfig->label}
		</div>
	{/foreach}
</div>
